<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% contacts.forEach(contact => {%>
      <li><%= contact.firstName %></li>
    <%})%>
    <hr>
    <form method="post" action="/contacts">
      <input type="text" name="firstName" placeholder="Ingresa el nombre(s)" required>
      <br>
      <input type="text" name="lastName" placeholder="Ingresa el apellido(s)" required>
      <br>
      <input type="email" name="email" placeholder="Ingrea el email" required>
      <br>
      <input type="tel" name="phoneNumber" placeholder="Ingresa el número de teléfono" required>
      <br>
      <button type="submit">Añadir contacto</button>
    </form>
  </body>
</html>
